steps:
  # Write .env.production so NEXT_PUBLIC_* vars are baked into the Next.js bundle at build time
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        cat > apps/web/.env.production <<EOF
        NEXT_PUBLIC_FIREBASE_API_KEY=$$NEXT_PUBLIC_FIREBASE_API_KEY
        NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=$$NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
        NEXT_PUBLIC_FIREBASE_PROJECT_ID=$$FIREBASE_PROJECT_ID
        NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=$$NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
        NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=$$NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
        NEXT_PUBLIC_FIREBASE_APP_ID=$$NEXT_PUBLIC_FIREBASE_APP_ID
        NEXT_PUBLIC_APP_URL=https://app.smartcommission.app
        EOF
    secretEnv:
      - NEXT_PUBLIC_FIREBASE_API_KEY
      - NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
      - FIREBASE_PROJECT_ID
      - NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
      - NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
      - NEXT_PUBLIC_FIREBASE_APP_ID

  # Build the Docker image (NEXT_PUBLIC_* baked in via .env.production above)
  - name: 'gcr.io/cloud-builders/docker'
    entrypoint: bash
    args:
      - '-c'
      - |
        DOCKER_BUILDKIT=1 docker build \
          -t $_REGION-docker.pkg.dev/$PROJECT_ID/smartcommission/app:$COMMIT_SHA \
          -t $_REGION-docker.pkg.dev/$PROJECT_ID/smartcommission/app:latest \
          --cache-from $_REGION-docker.pkg.dev/$PROJECT_ID/smartcommission/app:latest \
          --build-arg BUILDKIT_INLINE_CACHE=1 \
          .
    dir: 'apps/web'

  # Push the image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - push
      - '--all-tags'
      - '$_REGION-docker.pkg.dev/$PROJECT_ID/smartcommission/app'

  # Migrate + test: run Prisma migrations then the Jest integration test suite
  - name: 'node:22-slim'
    entrypoint: bash
    secretEnv:
      - DATABASE_URL
      - FIREBASE_PROJECT_ID
      - FIREBASE_CLIENT_EMAIL
      - FIREBASE_PRIVATE_KEY
      - NEXT_PUBLIC_FIREBASE_API_KEY
    env:
      - 'NODE_ENV=test'
      - 'TEST_BASE_URL=http://localhost:3000'
    args:
      - '-c'
      - |
        set -e

        # Install system deps
        apt-get update -qq && apt-get install -y --no-install-recommends curl ca-certificates

        # Download Cloud SQL Auth Proxy
        curl -so /usr/local/bin/cloud-sql-proxy \
          https://storage.googleapis.com/cloud-sql-connectors/cloud-sql-proxy/v2.14.1/cloud-sql-proxy.linux.amd64
        chmod +x /usr/local/bin/cloud-sql-proxy

        # Start proxy in background
        /usr/local/bin/cloud-sql-proxy --port 5433 $_INSTANCE_CONNECTION_NAME &
        PROXY_PID=$!

        # Rewrite DATABASE_URL to use the proxy TCP address
        export DATABASE_URL=$(node -e "
          const url = process.env.DATABASE_URL;
          const stripped = url.replace(/[?&]host=\/cloudsql[^&]*/g, '').replace(/[?&]\$/, '');
          const proxied = stripped.replace(/@[^/]*\//, '@127.0.0.1:5433/');
          process.stdout.write(proxied);
        ")

        sleep 5

        cd apps/web

        # Install dependencies
        npm ci

        # Generate Prisma client explicitly — postinstall hook may not fire in CI
        node node_modules/.bin/prisma generate --config prisma.config.ts

        # Run Prisma migrations
        node node_modules/.bin/prisma migrate deploy --config prisma.config.ts

        # Write NEXT_PUBLIC_* vars so Next.js can build
        cat > .env.production <<EOF
        NEXT_PUBLIC_FIREBASE_API_KEY=$$NEXT_PUBLIC_FIREBASE_API_KEY
        NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=smartcommission.firebaseapp.com
        NEXT_PUBLIC_FIREBASE_PROJECT_ID=$$FIREBASE_PROJECT_ID
        NEXT_PUBLIC_APP_URL=https://app.smartcommission.app
        EOF

        # Write server env so the running Next.js process can authenticate
        cat > .env.local <<EOF
        DATABASE_URL=$$DATABASE_URL
        FIREBASE_PROJECT_ID=$$FIREBASE_PROJECT_ID
        FIREBASE_CLIENT_EMAIL=$$FIREBASE_CLIENT_EMAIL
        FIREBASE_PRIVATE_KEY=$$FIREBASE_PRIVATE_KEY
        NEXT_PUBLIC_FIREBASE_API_KEY=$$NEXT_PUBLIC_FIREBASE_API_KEY
        NODE_ENV=test
        EOF

        # Build the Next.js app
        npm run build

        # Start standalone Next.js server
        (cd /workspace/apps/web/.next/standalone && PORT=3000 HOSTNAME=0.0.0.0 node server.js) &
        SERVER_PID=$!

        # Wait for the server to be ready (up to 60 s)
        for i in $(seq 1 30); do
          curl -sf http://localhost:3000/ > /dev/null 2>&1 && break
          sleep 2
        done

        # Run integration tests
        npm test -- --runInBand --forceExit
        TEST_EXIT=$?

        # Clean up
        kill $$SERVER_PID 2>/dev/null || true
        kill $$PROXY_PID 2>/dev/null || true

        exit $$TEST_EXIT

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - run
      - deploy
      - smartcommission
      - '--image=$_REGION-docker.pkg.dev/$PROJECT_ID/smartcommission/app:$COMMIT_SHA'
      - '--region=$_REGION'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--add-cloudsql-instances=$_INSTANCE_CONNECTION_NAME'
      - '--service-account=smartcommission-app@$PROJECT_ID.iam.gserviceaccount.com'
      - '--memory=1Gi'
      - '--cpu=1'
      - '--min-instances=0'
      - '--max-instances=5'
      - '--cpu-boost'
      - '--concurrency=80'
      - '--timeout=300'
      - '--set-env-vars=OIDC_ISSUER=https://app.smartcommission.app'
      - >-
        --set-secrets=DATABASE_URL=smartcommission-db-url:latest,SESSION_SECRET=smartcommission-session-secret:latest,ENCRYPTION_KEY=smartcommission-encryption-key:latest,CLEANUP_SECRET=smartcommission-cleanup-secret:latest,FIREBASE_PROJECT_ID=smartcommission-firebase-project-id:latest,FIREBASE_CLIENT_EMAIL=smartcommission-firebase-client-email:latest,FIREBASE_PRIVATE_KEY=smartcommission-firebase-private-key:latest,GEMINI_API_KEY=smartcommission-gemini-key:latest,STRIPE_SECRET_KEY=smartcommission-stripe-secret:latest,STRIPE_WEBHOOK_SECRET=smartcommission-stripe-webhook:latest,OXR_API_KEY=smartcommission-oxr-key:latest,RESEND_API_KEY=smartcommission-resend-key:latest,OIDC_PRIVATE_KEY_PEM=smartcommission-oidc-private-key:latest,OIDC_PUBLIC_KEY_PEM=smartcommission-oidc-public-key:latest

availableSecrets:
  secretManager:
    - versionName: 'projects/$PROJECT_ID/secrets/smartcommission-db-url/versions/latest'
      env: 'DATABASE_URL'
    - versionName: 'projects/$PROJECT_ID/secrets/smartcommission-firebase-api-key/versions/latest'
      env: 'NEXT_PUBLIC_FIREBASE_API_KEY'
    - versionName: 'projects/$PROJECT_ID/secrets/smartcommission-firebase-auth-domain/versions/latest'
      env: 'NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN'
    - versionName: 'projects/$PROJECT_ID/secrets/smartcommission-firebase-project-id/versions/latest'
      env: 'FIREBASE_PROJECT_ID'
    - versionName: 'projects/$PROJECT_ID/secrets/smartcommission-firebase-storage-bucket/versions/latest'
      env: 'NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET'
    - versionName: 'projects/$PROJECT_ID/secrets/smartcommission-firebase-messaging-sender-id/versions/latest'
      env: 'NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID'
    - versionName: 'projects/$PROJECT_ID/secrets/smartcommission-firebase-app-id/versions/latest'
      env: 'NEXT_PUBLIC_FIREBASE_APP_ID'
    - versionName: 'projects/$PROJECT_ID/secrets/smartcommission-firebase-client-email/versions/latest'
      env: 'FIREBASE_CLIENT_EMAIL'
    - versionName: 'projects/$PROJECT_ID/secrets/smartcommission-firebase-private-key/versions/latest'
      env: 'FIREBASE_PRIVATE_KEY'

substitutions:
  _REGION: 'australia-southeast1'
  _INSTANCE_CONNECTION_NAME: 'prakash-shared:australia-southeast1:shared-db-sydney'

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'
  substitutionOption: 'ALLOW_LOOSE'
